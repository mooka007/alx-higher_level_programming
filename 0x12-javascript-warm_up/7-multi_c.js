#!/usr/bin/node

const x = parseInt(process.argv[2]);
if (isNaN(x)) {
	console.log('Missing number of occurrences');
} else {
	jomla = "C if fun";
	let y;
	for (y = 0; y < x; y++) {
		console.log(jomla);
	}
}
