#!/usr/bin/node

const num = parseInt(process.argv[2]);
if (isNaN(num)) {
	console.log('Missing number of occurrences');
} else {
	jomla = "C if fun";
	let x;
	for (x = 0; x < num; x++) {
		console.log(jomla);
	}
}
